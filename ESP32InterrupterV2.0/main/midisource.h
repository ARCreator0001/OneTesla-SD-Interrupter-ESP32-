#ifndef __MIDISOURCE_H
#define __MIDISOURCE_H
#include <SD.h>
#include "datatypes.h"

void midisource_play(const char* path, const char* title);  // play a .mid file from SD, show title on LCD

#endif
